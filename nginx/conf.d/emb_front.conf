# Frontend - Configuration HTTP (redirection vers HTTPS)
server {
    listen 80;
    server_name emb_front.alicebot.me;

    # Certbot challenge pour SSL
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Redirection vers HTTPS
    location / {
        return 301 https://$server_name$request_uri;
    }
}

# Frontend - Configuration HTTPS
server {
    listen 443 ssl http2;
    server_name emb_front.alicebot.me;

    # Certificats SSL
    ssl_certificate /etc/letsencrypt/live/emb_front.alicebot.me/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/emb_front.alicebot.me/privkey.pem;

    # Configuration SSL sécurisée
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;
    ssl_ciphers 'ECDHE-RSA-AES256-GCM-SHA512:DHE-RSA-AES256-GCM-SHA512:ECDHE-RSA-AES256-GCM-SHA384:DHE-RSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-SHA384';
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;
    ssl_stapling on;
    ssl_stapling_verify on;

    # Security headers
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # Logs
    access_log /var/log/nginx/emb_frontend_access.log;
    error_log /var/log/nginx/emb_frontend_error.log;

    # Reverse proxy vers le frontend (Docker host)
    location / {
        # 172.17.0.1 est l'IP par défaut du Docker host depuis un conteneur
        proxy_pass http://172.17.0.1:3000;
        proxy_http_version 1.1;

        # Headers pour Next.js
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_cache_bypass $http_upgrade;

        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
}
